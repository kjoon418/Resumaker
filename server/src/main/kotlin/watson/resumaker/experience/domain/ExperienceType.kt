package watson.resumaker.experience.domain

/**
 * 경험 유형(도메인 이해 기능 1, 구현 설계 §3.3).
 * 프로젝트 / 직무·인턴 / 대외활동 / 수상·자격 / 학습·교육.
 */
enum class ExperienceType {
    PROJECT,
    JOB,
    EXTRACURRICULAR,
    AWARD,
    LEARNING,
}
